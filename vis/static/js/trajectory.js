class Trajectory {
    sequence;            
    unique_states;
    optimal_cluster_value;
    feasible_clusters;    
    clusterings = {};
    fuzzy_memberships = {};
    current_clustering;
    properties;
    raw;
    LAMMPSBootstrapScript;
}
